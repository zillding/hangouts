/**
*
* YoutubeNotice
*
*/

import React from 'react';

import styles from './styles.css';

const YoutubeNotice = () => (
  <p className={styles.youtubeNotice}>
    * Use above control buttons to make sure actions are sync across all users in the room.
  </p>
);

export default YoutubeNotice;
